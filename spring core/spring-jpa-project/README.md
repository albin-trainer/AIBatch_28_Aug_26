# spring-jpa-project

Sample Spring Boot project with Spring Web, Spring Data JPA and PostgreSQL.

Run with:

```bash
mvn spring-boot:run
```

Configure your Postgres settings in `src/main/resources/application.properties`.
